# Dependencies for worker_tray.py's system tray icon -- the worker MACHINE
# only (Windows/Mac), never the droplet. Kept separate from requirements.txt
# on purpose: the cloud agent has no display to put a tray icon on, and a
# `pip install -r requirements.txt` on the droplet should never pull in a
# GUI toolkit it can't use.
#
# Install once on the worker machine:
#   .venv\Scripts\pip install -r requirements-tray.txt
pystray>=0.19.5
Pillow>=10.0.0
